/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, 
    images : { 
      domains : ['www.cleanlink.com',"www.harpic.co.in"] 

    } 
};

export default nextConfig;
